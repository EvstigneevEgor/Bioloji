package com.company

/**
 * Состояние ячейки поля. Заменяет прежний набор перекрывающихся булевых
 * флагов (live/isdead/corpse), исключая противоречивые комбинации.
 *
 *  - [[Empty]]  — пусто, ячейку можно занять;
 *  - [[Alive]]  — живая клетка;
 *  - [[Corpse]] — труп (занимает ячейку, может быть съеден).
 */
enum CellState:
  case Empty, Alive, Corpse
